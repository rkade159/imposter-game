// Word source stub.
//
// Will load word lists from public/data/ (fetched at runtime as static JSON)
// and pick a word for each round. Returns an empty array for now so callers
// can be wired up against the real shape once the word-list brief lands.

export async function loadWords() {
  return [];
}
